{
  "name": "@cbronai/pensieve-mcp",
  "version": "0.1.0",
  "description": "MCP server — Pensieve hierarchical memory as MCP tools (Claude Desktop, Cursor, etc.)",
  "main": "dist/index.js",
  "bin": { "pensieve-mcp": "dist/index.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "keywords": ["mcp", "memory", "agent", "pensieve", "sqlite"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@cbronai/pensieve-local": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
