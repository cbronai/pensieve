{
  "name": "@cbronai/pensieve-local",
  "version": "0.1.0",
  "description": "Hierarchical memory layer for AI agents — local SQLite-backed, zero cloud dependency.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "keywords": ["ai", "memory", "agent", "llm", "sqlite", "pensieve"],
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^9.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.8",
    "typescript": "^5.0.0"
  }
}
